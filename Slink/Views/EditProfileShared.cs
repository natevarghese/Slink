using System;
using System.Threading.Tasks;
//using Acr.UserDialogs;

namespace Slink
{
    public class EditProfileShared
    {
        //async public Task UserProfileImage(Action downloadFacebook, Action selectFromGallery)
        //{
        //var buttons = new string[] { Strings.Alerts.user_facebook_image, Strings.Alerts.select_from_gallery };
        //var selection = await UserDialogs.Instance.ActionSheetAsync(Strings.Alerts.select_image_source, Strings.Basic.cancel, null, null, buttons);
        //if (String.IsNullOrEmpty(selection) || selection.Equals(Strings.Basic.cancel, StringComparison.OrdinalIgnoreCase)) return;

        //if (selection.Equals(Strings.Alerts.user_facebook_image, StringComparison.OrdinalIgnoreCase))
        //{
        //    downloadFacebook?.Invoke();
        //}
        //else if (selection.Equals(Strings.Alerts.select_from_gallery, StringComparison.OrdinalIgnoreCase))
        //{
        //    selectFromGallery?.Invoke();
        //}
        //}
    }
}
